name: Release

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  new-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
    steps:
      - uses: actions/checkout@v7.0.1

      # No registry-url here on purpose: it makes setup-node write an .npmrc
      # containing ${NODE_AUTH_TOKEN}, and `cache: yarn` then runs `yarn cache dir`,
      # where yarn 1 fails hard trying to expand a variable that is only defined on
      # the publish step. The registry is configured separately, just before publishing.
      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc
          cache: yarn

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Run tests
        run: yarn test

      - name: Conventional Changelog Action
        id: changelog
        if: github.event_name == 'push'
        uses: TriPSs/conventional-changelog-action@v6.4.0
        with:
          github-token: ${{ secrets.github_token }}

      - name: Create Release
        uses: softprops/action-gh-release@v3
        if: steps.changelog.outputs.skipped == 'false'
        with:
          tag_name: ${{ steps.changelog.outputs.tag }}
          name: ${{ steps.changelog.outputs.tag }}
          body: ${{ steps.changelog.outputs.clean_changelog }}
          draft: false
          prerelease: false

      # Writes the authenticated .npmrc. Kept out of the cached setup above, and run
      # after every yarn invocation, so nothing reads it before NODE_AUTH_TOKEN exists.
      - name: Configure npm registry
        if: steps.changelog.outputs.skipped == 'false'
        uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc
          registry-url: https://registry.npmjs.org

      # Only publish when the changelog step actually cut a new version. On a manual
      # workflow_dispatch that step is skipped, so `skipped` is empty and publishing
      # would retry a version npm already has.
      - name: Publish to npm
        if: steps.changelog.outputs.skipped == 'false'
        run: npm publish --access public --provenance
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
